{
  "name": "fit-fusion-api",
  "version": "1.0.0",
  "description": "Fit Fusion AI - Stripe API Backend",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "echo 'API routes ready'"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
